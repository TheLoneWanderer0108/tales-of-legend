#include <iostream>

 int main(){
    std::cout << "Tales of Legend";

    return 0;
 };

 //All errors where produced by the compiler

 //when compiling with link -c it gave me an .o file, when trying to run it it gave me permission denied, when permission are given it gives an error "cannot execute binary file"
 